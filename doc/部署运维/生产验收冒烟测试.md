# 生产验收冒烟测试

本文用于边端网关正式部署、OTA 升级后、现场故障恢复后的快速验收。目标是用一个脚本检查“能否安全进入生产运行”，不是替代长稳压测。

脚本路径：

```sh
/opt/modbus-gateway/bin/production-smoke-test.sh
```

推荐入口：

```sh
/opt/modbus-gateway/bin/gateway-run.sh smoke
```

## 1. 适用场景

- 新设备出厂配置完成后。
- 现场更换 MQTT broker、TLS 证书、串口配置后。
- 平台下发配置 OTA 后。
- 边端异常断电、重启、共享内存清理后。
- 正式生产前最后一轮检查。

## 2. 执行方式

默认检查：

```sh
/opt/modbus-gateway/bin/gateway-run.sh smoke
```

指定安装目录：

```sh
GATEWAY_HOME=/opt/modbus-gateway /opt/modbus-gateway/bin/production-smoke-test.sh
```

启用主动 MQTT 连接测试：

```sh
MQTT_CONNECT_TEST=1 /opt/modbus-gateway/bin/gateway-run.sh smoke
```

主动 MQTT 测试会执行 `MqttDriver --once`，可能向 broker 发布一次按需数据。生产现场默认不启用，避免误触发第三方平台数据处理。

## 3. 检查项

| 检查项 | 生产意义 | 失败处理 |
| --- | --- | --- |
| 身份配置 | 确认 `machineCode` 是平台登记的唯一网关标识，避免 topic 串台 | 重新执行带 `--machine-code` 的初始化，或修改 `config/runtime/device_identity.json` 后重启服务 |
| 运行文件 | 确认可执行文件和脚本齐全，避免 OTA 包漏文件 | 重新安装 OTA 包或执行出厂安装脚本 |
| 设备配置引用 | 确认 `deviceConfigFiles[]` 指向的 JSON 都存在 | 在平台重新生成配置包，或修正 app 配置中的路径 |
| MQTT TLS | 确认 TLS 已启用、OpenSSL 运行库存在、CA/客户端证书路径正确；本机 stunnel 方案还会校验 `accept/connect/checkHost/CAfile` | 补齐 `libssl/libcrypto`，优先重新执行 TLS enrollment 拉取 CA/客户端证书，或修正 broker scheme/stunnel 模板占位 |
| systemd 服务 | 确认需要运行的协议驱动、事件引擎、MQTT、监测服务处于 active | 查看 `journalctl`，修复配置后 `systemctl restart gateway-services.service` |
| 共享内存 | 确认 `pointctl stats/pending` 可返回，不存在锁死或写队列异常 | 停服务后清理 `/dev/shm/gateway_point_store*` 再启动 |
| 磁盘空间 | 防止 SQLite、MQTT ring、OTA 包把磁盘打满 | 清理旧 OTA 包、日志、无效缓存，或扩容 |
| 配置权限 | 避免 MQTT 密码、TLS 路径、身份信息被普通用户读取 | 建议 `chmod 600/640` |
| OTA 脚本 | 确认升级、回滚脚本存在且可执行 | 重新部署 `ota-apply.sh` 和 `ota-rollback.sh` |
| MQTT 主动连接 | 可选确认 broker 实际可连接 | 检查网络、账号密码、TLS 证书和 broker 端 ACL |

## 4. 通过标准

脚本最后输出：

```text
summary: pass=... warn=... fail=0
```

验收标准：

- `fail=0` 才允许进入生产。
- `warn` 允许存在，但必须人工确认原因。
- 生产跨公网或不可信网络时，非 TLS broker 警告不能忽略。
- `tls.insecureSkipVerify=true` 会被判定为失败。
- `ota.retentionCount > 3` 会被判定为失败。

## 5. 常见输出说明

`[PASS] pointctl stats completed`

说明共享内存可以被正常打开，内部互斥锁没有卡死。

`[FAIL] service not active: modbus-rtu@ttySP1.service`

说明当前配置要求这个串口驱动运行，但 systemd 中不是 active。优先看：

```sh
journalctl -u modbus-rtu@ttySP1.service -n 100 --no-pager
```

`[WARN] mqtt-service.json uses non-TLS broker`

说明 app 配置仍使用 `tcp://` broker。仅允许内网临时测试；跨公网或不可信网络必须改为 `ssl://`、`tls://`、`mqtts://` 或 `tls.enabled=true`。

`[FAIL] TLS CA file missing`

说明启用了 TLS 但 CA 文件路径不存在。优先执行：

```sh
GATEWAY_HOME=/opt/modbus-gateway sh /opt/modbus-gateway/bin/gateway-tls-enroll.sh \
  --machine-code <machineCode> \
  --platform-url https://<platform-host> \
  --token "$TLS_ENROLLMENT_TOKEN" \
  --generate-root-ca \
  --update-local-app \
  --restart
```

如果现场采用人工证书交付，再把 CA 放到配置声明路径，或修正 `tls.caFile`。

`[FAIL] stunnel connect still uses template placeholder`

说明 app broker 已指向本机 stunnel 端口，但 `config/runtime/tls/*-stunnel.conf` 仍是出厂模板。必须把 `connect`、`checkHost` 和 `CAfile` 改成真实生产 broker 与证书路径。

`[FAIL] pointctl stats failed or timed out`

说明共享内存可能损坏、旧进程未退出或互斥锁异常。处理方式：

```sh
systemctl stop gateway-services.service
/opt/modbus-gateway/bin/gateway-services.sh stop
rm -f /dev/shm/gateway_point_store*
systemctl start gateway-services.service
```

不要在服务运行时删除共享内存。

## 6. 可调参数

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `GATEWAY_HOME` | `/opt/modbus-gateway` | 边端安装目录 |
| `APP_CONFIG` | `$GATEWAY_HOME/config/runtime/apps/mqtt-service.json` | MQTT 服务 app 配置 |
| `MONITOR_CONFIG` | `$GATEWAY_HOME/config/runtime/apps/monitor-service.json` | 系统监测 app 配置 |
| `IDENTITY_CONFIG` | `$GATEWAY_HOME/config/runtime/device_identity.json` | 网关身份配置 |
| `MIN_FREE_MB` | `512` | 最小剩余磁盘空间 |
| `MAX_DISK_USED_PERCENT` | `85` | 最大磁盘使用率 |
| `MQTT_CONNECT_TEST` | `0` | 是否执行主动 MQTT 连接测试 |

## 7. 与长稳压测的边界

冒烟测试只判断“基础运行条件是否满足”。以下内容仍需要单独压测或现场验证：

- 8 路 485 同时采集时的 CPU、内存、串口时序。
- MQTT 断链 24 小时后的 ring/outbox 补传能力。
- OTA 下载中断、断电、脚本失败、回滚路径。
- TLS 双向证书在真实生产 broker 上的验收。
- 72 小时长稳运行后的磁盘、SQLite、日志增长。

## 8. 当前交叉编译产物记录

本记录用于定位当前仓库最新交叉编译产物，不等同于所有生产边端已完成替换。

### 8.1 2026-07-20 SCADA 双拓扑分支产物

以下是本次功能实际重新构建并同步到 Windows 初始化资源的产物：

| 产物 | 大小 bytes | 大小 KiB | sha256 |
| --- | ---: | ---: | --- |
| `MqttDriver` | 725872 | 708.9 | `cbd6ba571c8622f1008c9780ce3cd79ad3b17648e13db310524b786688c87ae5` |
| `SystemMonitor` | 1115936 | 1089.8 | `3b0911500b54116367f0edc3942069fce286aabf8fbc21deca6913cbf8194a56` |
| `KY-EMS` | 711296 | 694.6 | `9439c504899433f4f4a8224fa82dd56e80a7349029000b2f9b0be3f2ed984f85` |
| `gateway-factory-defaults.tar.gz` | 5172952 | 5051.7 | `0e332fbc7e60dd02acd5c2b1e9c2a74c24a2f3fcfe70fbac9eff441277e5ba1a` |

出厂包 `packageProfile=full`，已确认不含废弃的独立维护代理、AGC/AVC 独立运行模式、冲突文件、截图和临时构建目录。远程维护通过 MQTT 完成，本地网口维护由 `SystemMonitor` 内嵌接口完成。未在本次改动中重编译的协议驱动继续沿用下方完整产物记录，不能把本表理解为初始化包只包含 3 个程序。

### 8.2 2026-06-26 完整产物基线

2026-06-26 当前状态：

- 边端 C++ 驱动和工具已在 `192.168.22.11` 使用全志 aarch64 工具链编译通过。
- `gateway-factory-defaults.tar.gz` 已由当前 `build-aarch64` 产物重新生成。
- KY-EMS Qt 本地界面已在 `192.168.22.11` 编译，并在 `192.168.22.16` 通过 `ky-ems.service` 启动验证。
- KY-EMS 二进制已同步到桌面初始化包资源。

当前交叉编译产物大小：

| 二进制 | 大小 bytes | 大小 KiB | sha256 |
| --- | ---: | ---: | --- |
| `ModbusRtu` | 780600 | 762.3 | `0252b1de5079bf6f4dbb7c0e93fc616ee6cd0e4072d8c7b494fee9032e43cce3` |
| `Dlt645Driver` | 514704 | 502.6 | `f2ac2e3fc2e40ec9e4b052a73bcf08c9e5ffbea507e6d185cd184b2b1aa11a6e` |
| `DioDriver` | 483872 | 472.5 | `10f4342c3e77f77b7e2423a69435f6b378c0ba8e85bd3fc01c3c8e05509ad4f3` |
| `CanDriver` | 419792 | 410.0 | `7e55d18b80d48a38f75c1b1467246529aef543007fb402e53fb412ae4c8129ee` |
| `IecDriver` | 623080 | 608.5 | `c358a07e58f0d318d777e42af2efd2c49c4b7c0dc4204eca6c85000826e64490` |
| `MqttDriver` | 744320 | 726.9 | `1daab3bbeaad910c3c67abffd57d91f2bad47a95c06c81f05436e64dc07a46c2` |
| `EventEngine` | 608888 | 594.6 | `69c65072473af033f7651f63e8fd981dec02a2ddaec74030ca87b00c03aea3a9` |
| `ComputeEngine` | 713000 | 696.3 | `3082aa3e3eb658fc9a6ee082c9508c44398e189124e8a62fbe5e2e7cba727fae` |
| `SystemMonitor` | 891032 | 870.1 | `bd0f0a6e7d8d59ba5e4ba5cf6156c24321c53d2eafb55a5c09b60e1b9fbd7a88` |
| `pointctl` | 446824 | 436.4 | `865c154ae42381819fa4cf3df55e28e40e506498cde7cbfac5c57da6c138c6f6` |
| `QtDisplayBridge` | 376240 | 367.4 | `bd2ab37a6df54b9d7c0075d26d9c7852113885b7e2922965258a0184cfecda2b` |
| `LocalDisplay` | 407000 | 397.5 | `daee0984e6ef59cbd0676bea08c25d33365f6884e0e7ff42141d710349631353` |
| `CameraService` | 457584 | 446.9 | `54805d54dcc938b04ca42aaae2e3d181ce3ffc03ea1106a4390fbe4e0cd3a5a4` |
| `KY-EMS` | 8867664 | 8659.8 | `47b0fa243c294b9613078b989c1bc220f3fdb7e23dd5f68625c46ae03ba75b35` |

升级替换后建议停服务清理旧共享内存段，再启动新版本：

```sh
systemctl stop gateway-services.service
rm -f /dev/shm/gateway_point_store*
systemctl start gateway-services.service
```

不要在服务运行时删除共享内存。

## 9. 历史生产烟测记录

2026-06-08 记录保留用于追溯早期生产部署，不再作为当前二进制版本依据。

验证范围：

- 测试边端：`192.168.22.16`
- 生产边端：`10.126.126.2`
- 生产备份目录：`/opt/modbus-gateway/backup/replace-20260608190332`
- 测试备份目录：`/opt/modbus-gateway/backup/replace-20260608184040`

测试结论：

- `192.168.22.16`：`production-smoke-test.sh` 结果 `pass=48 warn=6 fail=0`
- `10.126.126.2`：`production-smoke-test.sh` 结果 `pass=58 warn=3 fail=0`
- 两端 `MQTT_CONNECT_TEST=1` 均通过，`MqttDriver --once completed`
- 生产端所有目标服务 `active`，`NRestarts=0`
- 生产端观察期内没有新的 `shared memory mutex lock timed out`、core dump、OOM、异常重启
- 生产端共享内存更新队列可被消费，最终观察 `pointUpdates=0/65535`

## 10. 2026-07-20 SCADA 双拓扑测试机烟测

验证范围仅为测试边端 `192.168.22.16 / COMM202600999`，没有部署 `10.126.126.*`：

- `production-smoke-test.sh`：`pass=77 warn=1 fail=0`。
- `MQTT_CONNECT_TEST=1` 主动连接通过，MQTT 双 TLS 已建立。
- broker 保持 `ssl://kygate.kyxn.net:8883`，未在本次测试中修改。
- 唯一 warning 为测试机 IMEI 为空；mqtt、monitor、identity 三个 JSON 权限已收紧为 `640`。
- 关键服务全部 `active`，`NRestarts=0`。
- Index `984` 质量正常，控制队列 `pending_count=0`。
- 当前 SCADA release 为 `ky-ems-COMM202600999-1.0.0-20260720172230`。
- 直连实时快照返回 1,001 点，上位机租约文件已生成且 machineCode 一致。
- `MqttDriver`、`SystemMonitor`、`KY-EMS` 均为 `active`，三者 `NRestarts=0`；`KY-EMS` 未因安全链部署而重启。
- 60 秒观察前后三者 PID 不变，SystemMonitor 与 MqttDriver 均有到 broker `:8883` 的已建立连接。
- 边端 22 个 C++ 测试、`scada_install_test.sh` 和 `scada_rollback_test.sh` 均通过。
- ARM64 安全链测试和真实断链测试通过。测试将临时上位机节点包的安全动作绑定到可写 DIO Index `984`，目标值使用现值 `1`，避免改变设备输出。
- 控制队列序号为 `7 -> 8 -> 8 -> 9`：断链安全动作入队一次，同一失联周期无重复；旧控制租约被拒绝且不入队，新鲜租约恢复后人工控制成功入队。
- 安全动作使用 `scada-offline-safety` 高优先级租约；新鲜租约控制设备写入 51ms、回读校验通过、总耗时 554ms。
- 测试后恢复原一体化 release，Index `984` 保持 `value=1 quality=1`，测试工程、租约、报文和远端备份均已清理。
- 旧 EMS 全量工程已部署：15 页、1,595 图元、1,158 Tag、57 资源，工程包 5,477,937 bytes，SHA-256 `abc18e3d6a93a9ca38997c34ee8710e4f5aeff558af2dd9f84ed18ef803e839f`。
- 使用 X11 事件遍历 15 个页面，1920x1080 截图均非空且哈希不同；未注入 mock 数据。
